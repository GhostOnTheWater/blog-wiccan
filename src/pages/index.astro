---
import BaseHead from "../components/BaseHead.astro";
import Footer from "../components/Footer.astro";
import Header from "../components/Header.astro";
import { SITE_TITLE } from "../consts";
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead
			title={SITE_TITLE}
			description="A chilling collection of stories, thoughts, and mysteries from the shadows. Enter if you dare."
		/>

		<style>
			:root {
				--hero-height: clamp(500px, 70vh, 700px);
			}

			body {
				margin: 0;
			}

			main {
				max-width: 1200px;
				margin: 0 auto;
				padding: 0 1.5rem 7rem;
			}

			/* HERO SECTION */
			.hero {
				position: relative;
				height: var(--hero-height);
				display: flex;
				align-items: center;
				justify-content: center;
				text-align: center;
				overflow: hidden;
				margin-bottom: 5rem;
				background: linear-gradient(
					180deg,
					var(--bg-secondary) 0%,
					var(--bg-primary) 100%
				);
			}

			.hero::before {
				content: "";
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				bottom: 0;
				background:
					radial-gradient(
						ellipse at 50% 20%,
						rgba(139, 0, 0, 0.15),
						transparent 50%
					),
					radial-gradient(
						ellipse at 80% 60%,
						rgba(74, 14, 78, 0.1),
						transparent 40%
					);
				animation: pulse-bg 8s ease-in-out infinite;
			}

			@keyframes pulse-bg {
				0%,
				100% {
					opacity: 0.6;
				}
				50% {
					opacity: 1;
				}
			}

			.haunted-mansion {
				position: absolute;
				bottom: 0;
				left: 50%;
				transform: translateX(-50%);
				width: 600px;
				max-width: 90%;
				height: 60%;
				opacity: 0.4;
				filter: drop-shadow(0 0 30px rgba(139, 0, 0, 0.4));
			}

			.haunted-mansion svg {
				width: 100%;
				height: 100%;
			}

			.fog-layer {
				position: absolute;
				bottom: 0;
				left: 0;
				right: 0;
				height: 200px;
				background: linear-gradient(
					to top,
					rgba(139, 0, 0, 0.1),
					transparent
				);
				animation: fog-drift 15s ease-in-out infinite;
			}

			@keyframes fog-drift {
				0%,
				100% {
					opacity: 0.3;
					transform: translateX(0);
				}
				50% {
					opacity: 0.6;
					transform: translateX(20px);
				}
			}

			.hero-content {
				position: relative;
				z-index: 10;
				padding: 2rem;
			}

			.hero h1 {
				font-size: clamp(3rem, 7vw, 6rem);
				margin: 0 0 1.5rem;
				letter-spacing: 0.08em;
				animation: flicker 4s ease-in-out infinite;
				text-shadow:
					0 0 20px rgba(139, 0, 0, 0.8),
					0 0 40px rgba(139, 0, 0, 0.6),
					2px 2px 8px rgba(0, 0, 0, 0.8);
			}

			@keyframes flicker {
				0%,
				100% {
					opacity: 1;
				}
				50% {
					opacity: 0.95;
				}
				51% {
					opacity: 1;
				}
				52% {
					opacity: 0.93;
				}
				53%,
				99% {
					opacity: 1;
				}
			}

			.hero p {
				max-width: 680px;
				margin: 0 auto;
				font-size: clamp(1.1rem, 2vw, 1.3rem);
				color: var(--text-secondary);
				line-height: 1.7;
			}

			.skull-divider {
				display: flex;
				align-items: center;
				justify-content: center;
				margin: 5rem 0;
				gap: 1.5rem;
			}

			.skull-divider::before,
			.skull-divider::after {
				content: "";
				height: 2px;
				width: 150px;
				background: linear-gradient(
					90deg,
					transparent,
					var(--accent-blood),
					transparent
				);
			}

			.skull-divider span {
				color: var(--accent-blood);
				font-size: 2rem;
				animation: float 3s ease-in-out infinite;
			}

			/* SECTIONS */
			.section {
				margin-bottom: 6rem;
			}

			.section h2 {
				font-size: clamp(2rem, 4vw, 2.8rem);
				margin-bottom: 1rem;
				color: var(--accent-blood-light);
				text-shadow: 1px 1px 3px rgba(139, 0, 0, 0.4);
			}

			.section p {
				max-width: 720px;
				color: var(--text-secondary);
				font-size: 1.1rem;
				line-height: 1.8;
			}

			/* CARD GRID */
			.grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
				gap: 2.5rem;
				margin-top: 3rem;
			}

			.card {
				background: linear-gradient(
					135deg,
					var(--bg-secondary),
					var(--bg-primary)
				);
				border: 1px solid var(--border-color);
				padding: 2.5rem 2rem;
				position: relative;
				overflow: hidden;
				transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
				box-shadow: var(--shadow-sm);
			}

			.card::before {
				content: "";
				position: absolute;
				inset: 0;
				background: linear-gradient(
					135deg,
					transparent 0%,
					rgba(139, 0, 0, 0.05) 50%,
					transparent 100%
				);
				opacity: 0;
				transition: opacity 0.4s ease;
			}

			.card::after {
				content: "";
				position: absolute;
				inset: -2px;
				background: linear-gradient(
					135deg,
					var(--accent-blood),
					var(--accent-purple)
				);
				opacity: 0;
				z-index: -1;
				transition: opacity 0.4s ease;
			}

			.card:hover {
				transform: translateY(-8px);
				box-shadow: var(--shadow-glow);
			}

			.card:hover::before {
				opacity: 1;
			}

			.card:hover::after {
				opacity: 0.3;
			}

			.card-category {
				color: var(--accent-blood-light);
				font-size: 0.75rem;
				letter-spacing: 0.2em;
				text-transform: uppercase;
				font-weight: 600;
				display: block;
				margin-bottom: 1rem;
			}

			.card h3 {
				margin: 0 0 1rem;
				font-size: 1.6rem;
				color: var(--text-primary);
				line-height: 1.3;
			}

			.card p {
				color: var(--text-secondary);
				font-size: 1rem;
				line-height: 1.6;
				margin: 0;
			}

			/* CLOSING MESSAGE */
			.closing {
				text-align: center;
				max-width: 720px;
				margin: 5rem auto 0;
				padding: 3rem;
				background: var(--bg-secondary);
				border: 2px solid var(--border-color);
				border-radius: 8px;
				position: relative;
				box-shadow: var(--shadow-md);
			}

			.closing::before {
				content: "🕯️";
				position: absolute;
				top: -20px;
				left: 50%;
				transform: translateX(-50%);
				font-size: 2rem;
				background: var(--bg-primary);
				padding: 0 1rem;
			}

			.closing p {
				font-size: 1.15rem;
				color: var(--text-secondary);
				line-height: 1.8;
				font-style: italic;
				margin: 0;
			}

			/* RESPONSIVE */
			@media (max-width: 720px) {
				.hero {
					height: 500px;
				}

				.grid {
					grid-template-columns: 1fr;
					gap: 2rem;
				}

				.skull-divider::before,
				.skull-divider::after {
					width: 80px;
				}
			}
		</style>
	</head>

	<body>
		<Header />

		<div class="hero">
			<div class="haunted-mansion">
				<svg viewBox="0 0 300 200" fill="currentColor" opacity="0.6">
					<!-- Haunted mansion silhouette -->
					<rect
						x="100"
						y="100"
						width="100"
						height="100"
						fill="var(--bg-primary)"></rect>
					<polygon
						points="150,60 100,100 200,100"
						fill="var(--bg-primary)"></polygon>
					<rect
						x="70"
						y="130"
						width="40"
						height="70"
						fill="var(--bg-primary)"></rect>
					<polygon
						points="90,120 70,130 110,130"
						fill="var(--bg-primary)"></polygon>
					<rect
						x="190"
						y="130"
						width="40"
						height="70"
						fill="var(--bg-primary)"></rect>
					<polygon
						points="210,120 190,130 230,130"
						fill="var(--bg-primary)"></polygon>
					<!-- Windows -->
					<rect
						x="120"
						y="120"
						width="20"
						height="25"
						fill="var(--accent-blood)"
						opacity="0.4"></rect>
					<rect
						x="160"
						y="120"
						width="20"
						height="25"
						fill="var(--accent-blood)"
						opacity="0.4"></rect>
					<rect
						x="140"
						y="155"
						width="20"
						height="30"
						fill="var(--accent-blood)"
						opacity="0.3"></rect>
				</svg>
			</div>
			<div class="fog-layer"></div>
			<div class="hero-content">
				<h1>Tales from the Shadows</h1>
				<p>
					Step into the darkness where stories are born from whispers
					and fears. A chilling collection of tales, thoughts, and
					mysteries that lurk in the corners of the mind.
				</p>
			</div>
		</div>

		<main>
			<div class="skull-divider">
				<span>☠</span>
			</div>

			<section class="section">
				<h2>Welcome to the Darkness</h2>
				<p>
					Here, the shadows hold secrets and every story has a
					heartbeat. This is where horror meets eloquence, where the
					macabre dances with the profound. Venture deeper if you
					dare.
				</p>
			</section>

			<section class="section">
				<h2>What Lurks Within</h2>

				<div class="grid">
					<article class="card">
						<span class="card-category">Horror Fiction</span>
						<h3>Nightmares in Prose</h3>
						<p>
							Original horror stories that crawl under your skin
							and take root in your dreams.
						</p>
					</article>

					<article class="card">
						<span class="card-category">Dark Thoughts</span>
						<h3>Whispers from the Void</h3>
						<p>
							Philosophical musings on existence, death, and the
							beautiful terror of being alive.
						</p>
					</article>

					<article class="card">
						<span class="card-category">Reviews</span>
						<h3>Chronicles of Fear</h3>
						<p>
							Deep dives into horror literature, films, and the
							art of scaring ourselves senseless.
						</p>
					</article>

					<article class="card">
						<span class="card-category">Creative Writing</span>
						<h3>The Craft of Terror</h3>
						<p>
							Insights on writing horror, building atmosphere, and
							making readers sleep with the lights on.
						</p>
					</article>
				</div>
			</section>

			<div class="skull-divider">
				<span>🕯️</span>
			</div>

			<section class="closing">
				<p>
					"We tell ourselves stories in order to live, but sometimes
					we tell them to remember what it feels like to be truly
					alive— heart pounding, breath held, standing at the edge of
					the abyss."
				</p>
			</section>
		</main>

		<Footer />
	</body>
</html>
