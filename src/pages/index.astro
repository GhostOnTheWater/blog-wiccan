---
import BaseHead from "../components/BaseHead.astro";
import Footer from "../components/Footer.astro";
import Header from "../components/Header.astro";
import { SITE_TITLE } from "../consts";
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead
			title={SITE_TITLE}
			description="Enter the void. A collection of nightmares, dark tales, and forbidden knowledge."
		/>

		<style>
			:root {
				--hero-height: 100vh;
			}

			body {
				margin: 0;
				cursor:
					url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20"><circle cx="10" cy="10" r="8" fill="rgba(139,0,0,0.5)" stroke="red" stroke-width="1"/></svg>')
						10 10,
					auto;
			}

			main {
				max-width: 1200px;
				margin: 0 auto;
				padding: 0 1.5rem 7rem;
			}

			/* EERIE PARTICLES */
			.particles {
				position: fixed;
				top: 0;
				left: 0;
				width: 100%;
				height: 100%;
				pointer-events: none;
				z-index: 1;
			}

			.particle {
				position: absolute;
				width: 2px;
				height: 2px;
				background: var(--accent-blood);
				border-radius: 50%;
				animation: float-particle 20s infinite linear;
				opacity: 0.3;
			}

			@keyframes float-particle {
				0% {
					transform: translateY(100vh) translateX(0);
					opacity: 0;
				}
				10% {
					opacity: 0.3;
				}
				90% {
					opacity: 0.3;
				}
				100% {
					transform: translateY(-100vh) translateX(100px);
					opacity: 0;
				}
			}

			/* HERO SECTION - ULTRA SCARY */
			.hero {
				position: relative;
				height: var(--hero-height);
				display: flex;
				align-items: center;
				justify-content: center;
				text-align: center;
				overflow: hidden;
				margin-bottom: 0;
				background: radial-gradient(
					ellipse at center,
					#0a0000 0%,
					#000000 100%
				);
			}

			/* Vignette effect */
			.hero::before {
				content: "";
				position: absolute;
				inset: 0;
				background: radial-gradient(
					ellipse at center,
					transparent 0%,
					rgba(0, 0, 0, 0.8) 100%
				);
				pointer-events: none;
				z-index: 2;
			}

			/* Animated grain/noise */
			.hero::after {
				content: "";
				position: absolute;
				inset: 0;
				background-image: url('data:image/svg+xml;utf8,<svg viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg"><filter id="noise"><feTurbulence type="fractalNoise" baseFrequency="0.9" numOctaves="4" stitchTiles="stitch"/></filter><rect width="100%" height="100%" filter="url(%23noise)" opacity="0.05"/></svg>');
				opacity: 0.4;
				animation: grain 0.5s steps(4) infinite;
				pointer-events: none;
				z-index: 3;
			}

			@keyframes grain {
				0%,
				100% {
					transform: translate(0, 0);
				}
				10% {
					transform: translate(-2%, -2%);
				}
				20% {
					transform: translate(2%, 2%);
				}
				30% {
					transform: translate(-2%, 2%);
				}
				40% {
					transform: translate(2%, -2%);
				}
				50% {
					transform: translate(-2%, -2%);
				}
				60% {
					transform: translate(2%, 2%);
				}
				70% {
					transform: translate(-2%, 2%);
				}
				80% {
					transform: translate(2%, -2%);
				}
				90% {
					transform: translate(-2%, -2%);
				}
			}

			/* Creepy eyes in darkness */
			.eyes {
				position: absolute;
				top: 20%;
				left: 15%;
				display: flex;
				gap: 30px;
				animation: blink 8s infinite;
				z-index: 4;
			}

			.eye {
				width: 12px;
				height: 12px;
				background: var(--accent-blood);
				border-radius: 50%;
				box-shadow: 0 0 20px var(--accent-blood);
				animation: eye-glow 3s ease-in-out infinite;
			}

			@keyframes blink {
				0%,
				48%,
				52%,
				100% {
					opacity: 1;
				}
				50% {
					opacity: 0;
				}
			}

			@keyframes eye-glow {
				0%,
				100% {
					filter: brightness(1);
				}
				50% {
					filter: brightness(1.5);
				}
			}

			/* Blood drip effect */
			.blood-drips {
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				height: 100%;
				pointer-events: none;
				z-index: 5;
			}

			.blood-drip {
				position: absolute;
				width: 2px;
				height: 0;
				background: linear-gradient(
					to bottom,
					var(--accent-blood),
					transparent
				);
				animation: drip-down 4s ease-in infinite;
			}

			@keyframes drip-down {
				0% {
					height: 0;
					opacity: 0;
				}
				10% {
					opacity: 1;
				}
				100% {
					height: 200px;
					opacity: 0;
				}
			}

			.hero-content {
				position: relative;
				z-index: 10;
				padding: 2rem;
			}

			/* Glitch effect on title */
			.hero h1 {
				font-size: clamp(3.5rem, 8vw, 7rem);
				margin: 0 0 2rem;
				letter-spacing: 0.1em;
				color: var(--text-primary);
				text-shadow:
					0 0 30px rgba(139, 0, 0, 1),
					0 0 60px rgba(139, 0, 0, 0.8),
					3px 3px 0 rgba(139, 0, 0, 0.5),
					-3px -3px 0 rgba(0, 255, 65, 0.2);
				animation: glitch 5s infinite;
				position: relative;
			}

			.hero h1::before,
			.hero h1::after {
				content: attr(data-text);
				position: absolute;
				left: 0;
				top: 0;
				width: 100%;
				height: 100%;
			}

			.hero h1::before {
				color: #ff0000;
				animation: glitch-1 0.3s infinite;
				clip-path: polygon(0 0, 100% 0, 100% 45%, 0 45%);
			}

			.hero h1::after {
				color: #00ff00;
				animation: glitch-2 0.3s infinite;
				clip-path: polygon(0 55%, 100% 55%, 100% 100%, 0 100%);
			}

			@keyframes glitch {
				0%,
				100% {
					transform: translate(0);
					text-shadow:
						0 0 30px rgba(139, 0, 0, 1),
						0 0 60px rgba(139, 0, 0, 0.8),
						3px 3px 0 rgba(139, 0, 0, 0.5);
				}
				25% {
					transform: translate(-2px, 2px);
					text-shadow:
						0 0 40px rgba(139, 0, 0, 1),
						0 0 80px rgba(139, 0, 0, 0.9),
						-3px 3px 0 rgba(0, 255, 65, 0.3);
				}
				50% {
					transform: translate(2px, -2px);
					text-shadow:
						0 0 35px rgba(139, 0, 0, 1),
						0 0 70px rgba(139, 0, 0, 0.85),
						3px -3px 0 rgba(139, 0, 0, 0.4);
				}
				75% {
					transform: translate(-1px, -1px);
					text-shadow:
						0 0 45px rgba(139, 0, 0, 1),
						0 0 90px rgba(139, 0, 0, 0.95),
						-3px -3px 0 rgba(0, 255, 65, 0.2);
				}
			}

			@keyframes glitch-1 {
				0% {
					transform: translate(0);
				}
				20% {
					transform: translate(-3px, 3px);
				}
				40% {
					transform: translate(-3px, -3px);
				}
				60% {
					transform: translate(3px, 3px);
				}
				80% {
					transform: translate(3px, -3px);
				}
				100% {
					transform: translate(0);
				}
			}

			@keyframes glitch-2 {
				0% {
					transform: translate(0);
				}
				20% {
					transform: translate(3px, -3px);
				}
				40% {
					transform: translate(3px, 3px);
				}
				60% {
					transform: translate(-3px, -3px);
				}
				80% {
					transform: translate(-3px, 3px);
				}
				100% {
					transform: translate(0);
				}
			}

			.hero p {
				max-width: 680px;
				margin: 0 auto;
				font-size: clamp(1.1rem, 2vw, 1.3rem);
				color: var(--text-secondary);
				line-height: 1.7;
				text-shadow: 0 0 10px rgba(0, 0, 0, 0.8);
				animation: fade-pulse 4s ease-in-out infinite;
			}

			@keyframes fade-pulse {
				0%,
				100% {
					opacity: 0.9;
				}
				50% {
					opacity: 1;
				}
			}

			/* Warning message */
			.warning {
				position: absolute;
				bottom: 10%;
				left: 50%;
				transform: translateX(-50%);
				color: var(--accent-blood);
				font-size: 0.9rem;
				letter-spacing: 0.3em;
				text-transform: uppercase;
				animation: warning-pulse 2s ease-in-out infinite;
				z-index: 10;
				text-shadow: 0 0 10px currentColor;
			}

			@keyframes warning-pulse {
				0%,
				100% {
					opacity: 0.5;
					transform: translateX(-50%) scale(1);
				}
				50% {
					opacity: 1;
					transform: translateX(-50%) scale(1.05);
				}
			}

			/* Scroll indicator */
			.scroll-indicator {
				position: absolute;
				bottom: 5%;
				left: 50%;
				transform: translateX(-50%);
				width: 30px;
				height: 50px;
				border: 2px solid var(--accent-blood);
				border-radius: 20px;
				z-index: 10;
			}

			.scroll-indicator::before {
				content: "";
				position: absolute;
				top: 10px;
				left: 50%;
				transform: translateX(-50%);
				width: 6px;
				height: 10px;
				background: var(--accent-blood);
				border-radius: 3px;
				animation: scroll-animation 2s infinite;
			}

			@keyframes scroll-animation {
				0% {
					opacity: 1;
					transform: translateX(-50%) translateY(0);
				}
				100% {
					opacity: 0;
					transform: translateX(-50%) translateY(20px);
				}
			}

			/* SECTIONS */
			.content-wrapper {
				background: var(--bg-primary);
				position: relative;
				padding-top: 6rem;
			}

			.skull-divider {
				display: flex;
				align-items: center;
				justify-content: center;
				margin: 5rem 0;
				gap: 1.5rem;
			}

			.skull-divider::before,
			.skull-divider::after {
				content: "";
				height: 2px;
				width: 150px;
				background: linear-gradient(
					90deg,
					transparent,
					var(--accent-blood),
					transparent
				);
			}

			.skull-divider span {
				color: var(--accent-blood);
				font-size: 2rem;
				animation: skull-float 3s ease-in-out infinite;
			}

			@keyframes skull-float {
				0%,
				100% {
					transform: translateY(0) rotate(0deg);
				}
				50% {
					transform: translateY(-10px) rotate(5deg);
				}
			}

			.section {
				margin-bottom: 6rem;
			}

			.section h2 {
				font-size: clamp(2rem, 4vw, 2.8rem);
				margin-bottom: 1rem;
				color: var(--accent-blood-light);
				text-shadow:
					1px 1px 3px rgba(139, 0, 0, 0.4),
					0 0 20px rgba(139, 0, 0, 0.3);
			}

			.section p {
				max-width: 720px;
				color: var(--text-secondary);
				font-size: 1.1rem;
				line-height: 1.8;
			}

			/* CARD GRID - Enhanced Horror */
			.grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
				gap: 2.5rem;
				margin-top: 3rem;
			}

			.card {
				background: linear-gradient(
					135deg,
					rgba(26, 26, 26, 0.8),
					rgba(10, 10, 10, 0.9)
				);
				border: 1px solid var(--border-color);
				padding: 2.5rem 2rem;
				position: relative;
				overflow: hidden;
				transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
				box-shadow:
					0 4px 20px rgba(0, 0, 0, 0.5),
					inset 0 0 30px rgba(139, 0, 0, 0.05);
			}

			.card::before {
				content: "";
				position: absolute;
				inset: 0;
				background: linear-gradient(
					135deg,
					transparent 0%,
					rgba(139, 0, 0, 0.1) 50%,
					transparent 100%
				);
				opacity: 0;
				transition: opacity 0.4s ease;
			}

			.card::after {
				content: "";
				position: absolute;
				inset: -2px;
				background: linear-gradient(
					135deg,
					var(--accent-blood),
					var(--accent-purple),
					var(--accent-blood)
				);
				opacity: 0;
				z-index: -1;
				transition: opacity 0.4s ease;
			}

			.card:hover {
				transform: translateY(-12px) scale(1.02);
				box-shadow:
					0 20px 40px rgba(139, 0, 0, 0.4),
					inset 0 0 50px rgba(139, 0, 0, 0.1);
			}

			.card:hover::before {
				opacity: 1;
			}

			.card:hover::after {
				opacity: 0.5;
			}

			.card-category {
				color: var(--accent-blood-light);
				font-size: 0.75rem;
				letter-spacing: 0.2em;
				text-transform: uppercase;
				font-weight: 600;
				display: block;
				margin-bottom: 1rem;
				text-shadow: 0 0 10px currentColor;
			}

			.card h3 {
				margin: 0 0 1rem;
				font-size: 1.6rem;
				color: var(--text-primary);
				line-height: 1.3;
				transition: all 0.3s ease;
			}

			.card:hover h3 {
				color: var(--accent-blood-light);
				text-shadow: 0 0 15px rgba(139, 0, 0, 0.6);
			}

			.card p {
				color: var(--text-secondary);
				font-size: 1rem;
				line-height: 1.6;
				margin: 0;
			}

			/* CLOSING MESSAGE */
			.closing {
				text-align: center;
				max-width: 720px;
				margin: 5rem auto 0;
				padding: 3rem;
				background: linear-gradient(
					135deg,
					rgba(26, 26, 26, 0.6),
					rgba(10, 10, 10, 0.8)
				);
				border: 2px solid var(--accent-blood);
				border-radius: 8px;
				position: relative;
				box-shadow:
					0 8px 32px rgba(139, 0, 0, 0.3),
					inset 0 0 50px rgba(139, 0, 0, 0.1);
			}

			.closing::before {
				content: "🕯️";
				position: absolute;
				top: -20px;
				left: 50%;
				transform: translateX(-50%);
				font-size: 2rem;
				background: var(--bg-primary);
				padding: 0 1rem;
				filter: drop-shadow(0 0 10px var(--accent-blood));
			}

			.closing p {
				font-size: 1.15rem;
				color: var(--text-secondary);
				line-height: 1.8;
				font-style: italic;
				margin: 0;
			}

			/* RESPONSIVE */
			@media (max-width: 720px) {
				.hero {
					height: 100vh;
				}

				.eyes {
					left: 10%;
					top: 15%;
				}

				.grid {
					grid-template-columns: 1fr;
					gap: 2rem;
				}

				.skull-divider::before,
				.skull-divider::after {
					width: 80px;
				}
			}
		</style>

		<script>
			// Particle system
			function createParticles() {
				const container = document.querySelector(".particles");
				if (!container) return;

				for (let i = 0; i < 30; i++) {
					const particle = document.createElement("div");
					particle.className = "particle";
					particle.style.left = Math.random() * 100 + "%";
					particle.style.animationDelay = Math.random() * 20 + "s";
					particle.style.animationDuration =
						15 + Math.random() * 10 + "s";
					container.appendChild(particle);
				}
			}

			// Blood drips
			function createBloodDrips() {
				const container = document.querySelector(".blood-drips");
				if (!container) return;

				const positions = [10, 30, 50, 70, 90];
				positions.forEach((pos, i) => {
					const drip = document.createElement("div");
					drip.className = "blood-drip";
					drip.style.left = pos + "%";
					drip.style.animationDelay = i * 0.8 + "s";
					container.appendChild(drip);
				});
			}

			// Initialize on load
			document.addEventListener("DOMContentLoaded", () => {
				createParticles();
				createBloodDrips();
			});

			// Extra creepy: subtle screen shake
			let shakeTimeout;
			function triggerShake() {
				document.body.style.animation = "shake 0.5s";
				setTimeout(() => {
					document.body.style.animation = "";
				}, 500);
			}

			// Random subtle shakes
			setInterval(() => {
				if (Math.random() > 0.95) {
					triggerShake();
				}
			}, 10000);

			// Add shake animation
			const style = document.createElement("style");
			style.textContent = `
				@keyframes shake {
					0%, 100% { transform: translate(0, 0); }
					10%, 30%, 50%, 70%, 90% { transform: translate(-1px, 0); }
					20%, 40%, 60%, 80% { transform: translate(1px, 0); }
				}
			`;
			document.head.appendChild(style);
		</script>
	</head>

	<body>
		<div class="particles"></div>

		<Header />

		<div class="hero">
			<div class="eyes">
				<div class="eye"></div>
				<div class="eye"></div>
			</div>
			<div class="blood-drips"></div>
			<div class="hero-content">
				<h1 data-text="TALES FROM THE SHADOWS">
					TALES FROM THE SHADOWS
				</h1>
				<p>
					The darkness beckons. Stories born from nightmares and
					forbidden knowledge await those brave—or foolish—enough to
					enter.
				</p>
			</div>
			<div class="warning">⚠ Enter at your own risk ⚠</div>
			<div class="scroll-indicator"></div>
		</div>

		<div class="content-wrapper">
			<main>
				<div class="skull-divider">
					<span>☠</span>
				</div>

				<section class="section">
					<h2>Welcome to the Abyss</h2>
					<p>
						You've crossed the threshold into shadow. Here, reality
						blurs and nightmares take form. Each story is a descent,
						each word a step deeper into darkness. There is no
						turning back.
					</p>
				</section>

				<section class="section">
					<h2>What Awaits in Darkness</h2>

					<div class="grid">
						<article class="card">
							<span class="card-category">Horror Fiction</span>
							<h3>Nightmares in Prose</h3>
							<p>
								Tales that will haunt your waking hours and
								poison your dreams. Enter if you dare.
							</p>
						</article>

						<article class="card">
							<span class="card-category">Dark Thoughts</span>
							<h3>Whispers from the Void</h3>
							<p>
								The void speaks. Its whispers reveal truths too
								terrible for the light of day.
							</p>
						</article>

						<article class="card">
							<span class="card-category">Reviews</span>
							<h3>Chronicles of Fear</h3>
							<p>
								Deep explorations of horror that will make you
								question what lurks in the shadows.
							</p>
						</article>

						<article class="card">
							<span class="card-category">Creative Writing</span>
							<h3>The Craft of Terror</h3>
							<p>
								Learn to weave nightmares into words and trap
								fear on the page.
							</p>
						</article>
					</div>
				</section>

				<div class="skull-divider">
					<span>🕯️</span>
				</div>

				<section class="closing">
					<p>
						"In the depths of night, when shadows grow long and
						whispers echo through empty halls, remember: you are
						never truly alone. Something is always watching."
					</p>
				</section>
			</main>
		</div>

		<Footer />
	</body>
</html>
