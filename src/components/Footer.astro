---
const today = new Date();
---

<footer>
	<div class="fog-effect"></div>
	<div class="footer-content">
		<p class="copyright">
			&copy; {today.getFullYear()} Tales from the Shadows. All rights reserved.
		</p>
		<p class="tagline">
			"In darkness, we find our voice. In shadows, our stories live
			eternal." 🕯️
		</p>
	</div>
</footer>
<style>
	footer {
		position: relative;
		padding: 4em 1.5em 3em;
		background: linear-gradient(
			180deg,
			var(--bg-primary) 0%,
			var(--bg-secondary) 50%,
			var(--bg-primary) 100%
		);
		border-top: 2px solid var(--accent-blood);
		text-align: center;
		overflow: hidden;
		margin-top: 5em;
	}

	.fog-effect {
		position: absolute;
		top: -50px;
		left: 0;
		right: 0;
		height: 100px;
		background: linear-gradient(
			180deg,
			transparent,
			rgba(139, 0, 0, 0.05),
			transparent
		);
		opacity: 0.6;
		animation: fog-float 10s ease-in-out infinite;
	}

	@keyframes fog-float {
		0%,
		100% {
			transform: translateY(0);
		}
		50% {
			transform: translateY(-10px);
		}
	}

	.footer-content {
		position: relative;
		z-index: 2;
	}

	.copyright {
		color: var(--text-secondary);
		font-size: 0.95em;
		margin-bottom: 1em;
		font-family: var(--font-body);
	}

	.tagline {
		color: var(--text-muted);
		font-style: italic;
		font-size: 0.9em;
		margin-top: 1.5em;
		max-width: 600px;
		margin-left: auto;
		margin-right: auto;
	}

	@media (max-width: 720px) {
		footer {
			padding: 3em 1em 2em;
		}
	}
</style>
